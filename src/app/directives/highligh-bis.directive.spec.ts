import { HighlighBisDirective } from './highligh-bis.directive';

describe('HighlighBisDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighBisDirective();
    expect(directive).toBeTruthy();
  });
});
